# Description:  Remove all measurement objects in the interal GUI.
# Source:  placeHolder

"""
cmd.do('cmd.do("delete measure*")')
cmd.do('cmd.do("delete m*_*")')
cmd.do('cmd.do("delete dist*")')
"""

cmd.do('cmd.do("delete measure*")')
cmd.do('cmd.do("delete m*_*")')
cmd.do('cmd.do("delete dist*")')
