# Description:  Print in the command history window the docstrings of all of the functions in the cmd module.
# Source:  placeHolder

"""
cmd.do('[help(i) for i in dir(cmd)];')
"""

cmd.do('[help(i) for i in dir(cmd)];')
