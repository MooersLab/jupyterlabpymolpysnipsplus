"""
cmd.do('list = setting.get_name_list();[print("%s => %s" % (name, setting.get_setting_text(name))) for name in list];')
"""
cmd.do('list = setting.get_name_list();[print("%s => %s" % (name, setting.get_setting_text(name))) for name in list];')

# Description:  Print to the screen the settings and their current parameter values.
# Source:  https://sourceforge.net/p/pymol/mailman/message/7562122/

