"""
cmd.do('[print("%s => %s" % (name, setting.get_setting_text(name))) for name in setting.get_name_list()];')
"""
cmd.do('[print("%s => %s" % (name, setting.get_setting_text(name))) for name in setting.get_name_list()];')

# Description:  Print to the screen the settings and their current parameter values. This is the more compact version.
# Source:  https://sourceforge.net/p/pymol/mailman/message/7562122/

